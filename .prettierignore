node_modules/
dist/
coverage/
package-lock.json
.env
.env.*
# ЄДИНИЙ prettier на весь репозиторій (H2). Раніше тут стояло `web/app/` з
# поміткою «форматується власним prettier пізніше» — того prettier так і не
# зʼявилось, тож 48 файлів дашборда лишались неформатованими, а format:check у
# CI на них мовчав. Другий інструмент із власним конфігом тут нічого не дав би:
# конфіг був би той самий, а розсинхрон рано чи пізно — ні.
#
# 1,4 МБ згенерованого датасету (web/scripts/gen-settlements.mjs пише його
# JSON.stringify без відступів) — виняток: розгорнутий він розпух би на
# порядок, і кожна регенерація знову робила б файл «неформатованим».
web/app/public/settlements.json
# зібраний артефакт React-дашборда (хешовані імена, мініфіковано) — не форматуємо
web/public/app/
# сторонні скіли (github.com/nextlevelbuilder/ui-ux-pro-max-skill), скопійовані
# як є — не форматуємо, щоб не ускладнювати майбутню звірку з апстрімом
.claude/skills/
# інструкції асистента - ТЕКСТ ДЛЯ МОДЕЛІ, не код. Prettier тут не косметика:
# він переносить рядки й вирівнює таблиці, від чого тіло persona.md розпухло з
# 4249 до 5668 символів (+33 % промпту на кожен прогін) і, головне, змінився
# sha256 тіла - тобто version_hash у D1 розійшовся б із файлом сам собою.
# Формат цих файлів визначає автор; перевіряє їх валідатор, не форматер.
docs/assistant/
