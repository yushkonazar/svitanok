# Дані, строки й стирання

Статус: реалізовано 2026-09-16. Документ описує межі зберігання застосунку,
а не політику сторонніх платформ.

## Матриця

| Межа                                         | Дані                                                                                                                  | Звичайна ретенція                                                                                                                                                                                                                                                                                                                                                                                                          | `forget all`                                                                                                                                                           | Доказ виконання                              |
| -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| D1                                           | предметні записи, `fact_ledger`, `runs`, `memory_chunks`, `sessions`                                                  | за таблицями в `retention/cleanup.mjs`; телеметрія/памʼять — 90 діб                                                                                                                                                                                                                                                                                                                                                        | `DELETE` owner-таблиць, FTS і state-полів після зовнішніх кроків                                                                                                       | лічильники квитанції T2                      |
| KV / StateStore / control-plane DO           | статистика, збережене, гео, UI-state, proposal, `/clear` ids, chat context, public job excerpts, mail triage metadata | за правилами конкретного ключа; `stats.briefingEngagement` — 120 денних агрегатів без контенту; `stats.jobSeen` — до 250 public URL/title вакансій, 30 діб; `stats.learningAttempts` — до 180 явно повідомлених outcomes з короткою темою, 90 діб; `state.jobDescriptions` — максимум 60 нормалізованих public excerpts, 14 діб; `state.mailTriage` — до 60 Gmail candidates, 3 доби; T2-квитанції — 90 діб, до 20 записів | owner-ключі, state-поля (включно з `jobDescriptions` і `mailTriage`) й canonical proposal/messages/history slots очищуються; службові ключі не містять тексту власника | `dataDeletionReceipt` + приватна історія     |
| RunRegistry DO                               | черги повідомлень і активні run id                                                                                    | короткоживучі; сторож run                                                                                                                                                                                                                                                                                                                                                                                                  | усі черги очищуються атомарно; активні run отримують abort                                                                                                             | `queues.count` у квитанції                   |
| Vectorize                                    | вектори `memory_chunks`                                                                                               | 90 діб, **Vectorize → D1**                                                                                                                                                                                                                                                                                                                                                                                                 | усі `vector_id` з D1 видаляються до D1                                                                                                                                 | `vectors.count` у квитанції                  |
| VPS Claude SDK                               | повні локальні транскрипти                                                                                            | 90 діб, `sdk_session_id` стає `NULL`, summary лишається                                                                                                                                                                                                                                                                                                                                                                    | `/sessions/delete` з HMAC, до видалення D1                                                                                                                             | `sdkSessions.count` у квитанції              |
| Google Drive                                 | лише app-owned `svitanok-YYYY-MM-DD.enc`                                                                              | 90 діб, безповоротний Drive `DELETE`                                                                                                                                                                                                                                                                                                                                                                                       | усі точно названі app-owned backup-файли видаляються безповоротно                                                                                                      | `backups.count` у квитанції                  |
| Telegram / Cloudflare / Google / VPS журнали | платформні журнали та вже надіслані повідомлення                                                                      | за політикою постачальника; застосунок їх не контролює                                                                                                                                                                                                                                                                                                                                                                     | не є доказом повного стирання в межах постачальника                                                                                                                    | окреме звернення до постачальника за потреби |

## T2 «забудь усе»

Після підтвердження зі словом створюється KV-квитанція `dataDeletionReceipt`.
Вона містить лише час, статуси та лічильники — ніколи id сесій, векторів,
повідомлень або текст даних. Поточна квитанція лишається ключем scheduler-а,
а окремі записи історії тримаються до 90 діб (не більше 20) і читаються лише
власником через `GET /api/deletions`; публічний `/api/status` їх ніколи не
бачить. У UI помилка віддається категорією без URL, stack чи id. Послідовність
незмінна:

1. RunRegistry очищає черги; якщо модель ще працює, ядро надсилає abort.
2. Після завершення активного прогону `deletion-reconcile` автоматично
   продовжує T2 (не потрібне друге слово).
3. VPS SDK-транскрипти, Vectorize й Drive-backups видаляються та підтверджуються.
4. Лише тоді очищуються D1, FTS, KV, owner-поля StateStore і canonical proposal/messages/history slots.

Збій зовнішнього кроку залишає локальні адресати на місці та ставить квитанцію
`failed`; відповідь не може назвати стирання завершеним. Єдиний виняток —
`waiting_for_active_runs`: це безпечне відкладення, яке scheduler повторює
кожні пʼять хвилин.

## Межі й операційна примітка

- `drive.file` дозволяє додатку бачити лише власні файли. Cleanup навмисно
  фільтрує точний шаблон backup-імені та не створює тек під час видалення.
- Якщо існує факт `last_backup`, але Drive/OAuth недоступний, T2 зупиняється:
  локальні дані не стираються з хибною заявою про повний scope.
- OAuth refresh-token, Cloudflare Secrets і Telegram-історія поза даними
  застосунку. Відкликання доступів або видалення платформних логів потребує
  окремої дії у відповідному сервісі.
- `summary_md` — коротка D1-згортка, а не SDK-транскрипт; його строк окремо
  визначає D1 policy. Повний VPS-транскрипт не зберігається довше 90 діб.
